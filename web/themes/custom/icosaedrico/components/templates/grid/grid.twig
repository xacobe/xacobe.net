{% set grid_classes = [
  "grid",
  "gap-" ~ grid_gap,
] %}

{% set grid_classes_dummy = [
  "md:grid-cols-2",
  "lg:grid-cols-3",
  "gap-" ~ grid_gap,
] %}

{% set grid_classes = grid_classes|merge([
  "grid-cols-" ~ grid_cols,
  "md:grid-cols-" ~ (grid_cols + 1),
  "lg:grid-cols-" ~ (grid_cols + 2),
]) %}

<section {{ attributes }} class="bg-white bg-gray-900">
<div class="px-6 py-10 mx-auto">
  <div class="flex items-center justify-between">
    {% if title %}
      <h1 class="text-2xl font-semibold text-gray-800 capitalize lg:text-3xl text-white">{{ title }}</h1>
    {% endif %}
    <button class="focus:outline-none">
      <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-gray-600 transition-colors duration-300 transform text-gray-400 hover:text-blue-500" fill="none" viewbox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
      </svg>
    </button>
  </div>
  <hr
  class="my-8 border-gray-200 border-gray-700">

  {# Grid cell #}
  <{{grid_tag_item}} class="{{ grid_classes|join(' ') }}">
    {% for row in rows %}
      <li>
        {% block grid_content %}{% endblock %}
      </li>
    {% endfor %}
    <{{grid_tag_item}}
  </div>
</section>
